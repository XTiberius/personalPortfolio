# David Rashid - Personal Portfolio

A static personal portfolio website built with React, Vite, and Tailwind CSS.

## Running the app

```bash
npm install
npm run dev
```

## Building the app

```bash
npm run build
```

The build output will be in the `/dist` directory, ready for static hosting (e.g., GitHub Pages).